import {LOAD_TOTAL_ORDER} from '../actionTypes';

const totalorder = (state = [], action) => {
// 	// console.log("action"+action);
// 	switch(action.type) {
// 		case LOAD_TOTAL_ORDER:
// 			return [...action.totalorder];
// 		default:
// 			return state;
// 	}
};

export default totalorder;